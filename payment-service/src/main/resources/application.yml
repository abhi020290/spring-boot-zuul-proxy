server:
  port: 8082
spring:
  application:
      name: PAYMENT-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      deafult-zone: http://localhost:8761/eureka
  instance:
    name: localhost

