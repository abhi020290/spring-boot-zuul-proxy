server:
  port: 8083
spring:
  application:
      name: ORDER-SERVICE
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      deafult-zone: http://localhost:8761/eureka
  instance:
    name: localhost

